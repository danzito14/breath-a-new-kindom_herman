import { SoloNumerosCode } from './solo-numeros-code';

describe('SoloNumerosCode', () => {
  it('should create an instance', () => {
    const directive = new SoloNumerosCode();
    expect(directive).toBeTruthy();
  });
});
