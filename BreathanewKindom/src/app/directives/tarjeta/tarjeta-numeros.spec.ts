import { TarjetaNumeros } from './tarjeta-numeros';

describe('TarjetaNumeros', () => {
  it('should create an instance', () => {
    const directive = new TarjetaNumeros();
    expect(directive).toBeTruthy();
  });
});
