import { TarjetaCvv } from './tarjeta-cvv';

describe('TarjetaCvv', () => {
  it('should create an instance', () => {
    const directive = new TarjetaCvv();
    expect(directive).toBeTruthy();
  });
});
